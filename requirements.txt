# 림프칩 PINN 의존성

# 핵심 라이브러리
torch>=2.0.0
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0

# 엑셀 파일 처리
openpyxl>=3.0.0

# 시각화
matplotlib>=3.5.0
plotly>=5.0.0

# 웹 앱
streamlit>=1.20.0

# 설정 파일
pyyaml>=6.0

# 선택적: GPU 가속 (CUDA)
# torch 설치 시 CUDA 버전에 맞게 설치
# pip install torch --index-url https://download.pytorch.org/whl/cu118

# 선택적: 실험 추적
# wandb>=0.15.0
# tensorboard>=2.13.0
